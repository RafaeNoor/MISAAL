# Progress of Implementation


## Research Concepts

* Synthesizing optimizations across architectures. Offline use of abstracted optimizations then generation of complex rewrites. Generation of multi-instructions. Complex equivalent instructions patterns which appear across targets. Higher level building blocks for programming. 

* Avoid enumeration of optimization discovery using equivalence classes


* Automatic support for layout and swizzle optimizations globally

## Tasks

* Identify Swizzles across targets

* Identify swizzle optimizations for movement of operations

* Identify swizzle optimizations for simplification

* More general emit_grammar which takes path to dictionary as a parameter 
